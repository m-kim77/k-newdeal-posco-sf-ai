# 포스코 Vision AI & Agent로 풀어내는 스마트팩토리 & 안전 시스템

## 전체 핵심 요약

이 발표에서 가장 중요한 메시지는 크게 5개입니다.

1. **제조 현장의 AI는 모델 성능만 좋다고 끝나는 게 아니다.**
   - 카메라 위치
   - 공장 환경
   - 서버 성능
   - 네트워크
   - PLC 연동
   - 운영 안정성
   - 현업 요구사항
   - 비용  
   까지 모두 고려해야 실제로 쓸 수 있습니다.

2. **연구소의 POC와 실제 현장 적용은 완전히 다르다.**
   - 연구소: “기술적으로 가능한가?”
   - 사업/현장: “몇 년 동안 안정적으로 운영 가능한가?”

3. **Vision AI는 점점 카메라 단독에서 센서 융합으로 발전하고 있다.**
   - 카메라만으로 100% 인식은 현실적으로 불가능
   - LiDAR 등 다른 센서를 함께 사용해 신뢰도를 높이는 방향

4. **Agent AI는 LLM 하나가 모든 것을 하는 구조가 아니라 여러 Agent와 Tool을 조합하는 방향이다.**
   - LangChain / LangGraph
   - Main Agent
   - Sub Agent
   - Tool Calling
   - Orchestration
   - Harness Engineering
   - PLC/설비 연동

5. **현장 AI 개발자에게는 코딩 실력만큼 현장 이해·소통·문서화 능력이 중요하다.**

---

## 1. 발표자의 업무와 커리어 이야기

발표자는 처음부터 Vision AI 담당으로 입사한 것은 아닙니다.

처음에는 **웹 개발 업무**를 하면서 연구소의 Vision/AI 개발자들과 협업했습니다.

그 과정에서 연구소 사람들에게 계속 질문하면서

> “실제 현장에서는 AI를 이렇게 적용하는구나.”

를 배웠고, 개인적으로 Vision AI를 준비하다가 회사 내 AI 인력이 부족해지면서 실제 Vision/AI 업무를 맡게 된 것으로 설명합니다.

즉,

**웹 개발 → AI 연구진과 협업 → 개인적으로 AI 준비 → Vision/AI 프로젝트 참여**

라는 흐름입니다.

발표자가 취업 준비생에게 강조한 것도 이것입니다.

> 처음부터 원하는 직무가 아니더라도 자신의 강점을 살려 회사에 들어가고, 이후 관심 분야의 역량을 계속 쌓으면 기회가 올 수 있다.

---

## 2. 발표에서 소개한 핵심 프로젝트 2개

발표에서는 수많은 프로젝트 중 교육생 커리큘럼과 관련 있는 두 가지를 선정했습니다.

### 프로젝트 A
**제철소 철도 건널목 Vision AI 안전관제**

### 프로젝트 B
**AI Operator 기반 설비 자동화 제어**

두 프로젝트를 한 줄로 정리하면:

> **Vision AI로 현장을 보고 → Agent AI가 판단하고 → 설비까지 제어하는 방향**

이라고 볼 수 있습니다.

---

## 3. 교육용 데이터와 실제 제조 데이터의 차이

교육에서는 보통 비교적 정리된 데이터가 주어집니다.

```text
데이터 제공
↓
이상치 제거
↓
분석
↓
모델 개발
↓
결론
```

하지만 실제 현장에서는 시작부터 다릅니다.

```text
엄청나게 많은 데이터
↓
도대체 어떤 데이터를 써야 하지?
↓
필요한 데이터 탐색
↓
데이터 품질 확인
↓
현장 의미 확인
↓
모델 개발
```

즉, 현장에서는 **“어떤 모델을 쓸 것인가?”보다 “어떤 데이터를 써야 하는가?”가 먼저 문제**가 될 수 있습니다.

또 주의해야 할 점은 원하는 결과에 맞춰 데이터를 끼워 맞추지 않는 것입니다.

---

## 4. 포스코에서 온프레미스 AI가 중요한 이유

제철소 데이터는 외부로 유출되면 안 되는 중요한 데이터입니다.

따라서 일반적인 Cloud AI를 마음대로 사용할 수 없습니다.

그래서 방향이

```text
공장 데이터
    ↓
사내 폐쇄망
    ↓
온프레미스 서버
    ↓
사내 AI / LLM
```

이 되는 것입니다.

즉,

> **데이터를 외부로 보내지 않고 회사 내부에서 AI를 돌린다.**

는 것입니다.

발표 후반 Q&A에서는 오픈소스 모델을 활용한다고 설명합니다.

대략적인 구조는

```text
오픈소스 LLM
      ↓
포스코 연구소
      ↓
Fine-tuning / Post-training
      ↓
포스코 공정에 특화된 모델
      ↓
온프레미스 환경에서 사용
```

입니다.

---

## 5. 왜 엣지 서버를 사용하는가

예전에는 좋은 GPU 서버를 크게 구성해서 사용했습니다.

그래서 최적화가 조금 부족해도

> “서버가 좋으니까 그냥 돌아간다.”

가 가능했습니다.

하지만 지금은 비용이 굉장히 중요해졌다고 설명합니다.

예를 들어 단순 Vision AI는 거대한 GPU 서버가 필요하지 않을 수도 있습니다.

그래서

```text
고성능 중앙 GPU 서버
```

대신

```text
공장 근처의 작은 Edge Server
```

를 사용하는 경우가 늘어났습니다.

특히 Vision AI는 비교적 작은 모델로도 충분한 성능을 낼 수 있기 때문에 Edge 환경에 적합합니다.

반대로 LLM + Agent가 들어가는 AI Operator 같은 시스템은 더 높은 서버 성능이 필요합니다.

---

## 6. GPU Slice와 최적화

포스코 내부 서버를 사용할 때 서버 전체를 한 프로젝트가 독점하는 것이 아니라 자원을 나눠줄 수 있다고 합니다.

예를 들어

```text
GPU 전체
████████████████

프로젝트 A
████

프로젝트 B
████

프로젝트 C
████

프로젝트 D
████
```

처럼 CPU/GPU/RAM 일부만 할당받습니다.

발표에서는 이것을 **Slice된 환경**이라고 표현합니다.

그러면 개발자가 사용할 수 있는 자원이 제한됩니다.

그래서 중요한 것이

> **모델 최적화**

입니다.

예를 들어 같은 하드웨어에서

```text
최적화 전
CCTV 3대 처리

최적화 후
CCTV 6대 처리
```

가 가능하다면 서버를 추가 구매하지 않아도 됩니다.

따라서 실제 산업에서는

> **최고 정확도의 모델 ≠ 최고의 모델**

일 수 있습니다.

오히려

> **제한된 하드웨어에서 충분한 정확도와 처리량을 내는 모델**

이 더 좋은 모델일 수 있습니다.

---

## 7. 제조 현장에서 Vision AI가 어려운 이유

학교 프로젝트와 가장 큰 차이가 여기입니다.

공장 외부 카메라에는 굉장히 많은 변수가 있습니다.

### 대표적인 문제

- 분진
- 먼지
- 비
- 눈
- 야간
- 역광
- 반사광
- 카메라 화질
- 카메라 화각
- 카메라 설치 위치
- 기존 노후 카메라

예를 들어 개발자는

```text
여기에 카메라 설치하면
사람이 잘 보입니다.
```

라고 생각해도 실제 현장에서는

```text
전선 설치 불가
설비 간섭
안전 문제
공사 비용
기존 인프라 문제
```

등으로 원하는 위치에 설치하지 못할 수 있습니다.

그래서 실제 현장을 직접 확인하는 것이 매우 중요하다고 합니다.

---

## 8. 왜 Vision AI에서 100%가 어려운가

현업에서는 점점

> “무조건 잡아주세요.”

라는 요구가 많아지고 있다고 합니다.

하지만 카메라 자체에서 물체가 보이지 않으면 AI도 인식할 수 없습니다.

예를 들어

```text
폭우
+
분진
+
역광
+
객체 가림
```

상황에서 사람이 눈으로도 보기 어렵다면 Vision AI도 어렵습니다.

그래서 발표자는 현업에 단순히

> “안 됩니다.”

라고 말하면 안 된다고 강조합니다.

대신

> 정상 환경에서는 높은 성능을 낼 수 있지만 특정 악조건에서는 오인식 가능성이 있고, 그 경우 다른 프로세스로 보완하겠습니다.

라고 설명해야 합니다.

즉,

### AI 개발 능력 + 기술적 한계를 설명하는 능력

둘 다 중요합니다.

---

## 9. Sensor Fusion이 필요한 이유

카메라의 한계를 해결하기 위해 최근에는 다른 센서를 결합합니다.

예를 들어

```text
Camera
   +
LiDAR
   +
기타 Sensor
   ↓
Sensor Fusion
   ↓
더 높은 신뢰도
```

입니다.

카메라가 잘 보지 못하는 상황을 LiDAR 같은 센서가 보완하는 방식입니다.

발표 내용상 연구소에서 먼저 관련 기술을 확보하고, 이후 사업실이 기술을 전달받아 현장 적용·확산하는 구조입니다.

---

## 10. 연구소와 사업실의 차이

### 연구소

목표:

> **기술적으로 가능한가?**

주로

- 새로운 기술 연구
- 모델 개발
- POC
- Sensor Fusion 연구

등을 수행합니다.

### 사업실

목표:

> **실제 공장에서 운영 가능한가?**

주로

- 현장 적용
- 서버 구축
- 설비 연동
- 안정화
- 유지보수
- 확산

을 담당합니다.

그래서 연구소에서는

> “POC 결과 잘 나왔습니다.”

라고 해도 사업실에서는

> “비 오는 날은?”  
> “밤에는?”  
> “카메라 위치가 바뀌면?”  
> “GPU가 이것밖에 없으면?”  
> “1년 계속 돌려도 안 죽나?”

를 확인합니다.

따라서 연구소의 모델을 그대로 현장에 가져가는 것은 어렵습니다.

---

## 11. 연구소의 가장 큰 가치 = 시행착오 감소

발표자가 연구소 기술을 받을 때 단순히

> “이 모델 쓰면 되나요?”

라고 묻지 않는다고 합니다.

대신

> 왜 이 모델을 선택했나요?  
> 다른 모델은 어떤 것을 테스트했나요?  
> 어떤 모델에서 성능이 안 나왔나요?  
> 어떤 시행착오가 있었나요?

를 묻습니다.

왜냐하면 이 정보를 알면 사업실에서 똑같은 실패를 반복하지 않아도 되기 때문입니다.

그래서 발표자는 연구소의 역할 중 하나를

> **현장 개발자의 시행착오를 줄여주는 것**

으로 봅니다.

---

## 12. AI Operator와 현장 작업자의 암묵지

AI Operator에서 매우 중요한 문제가 **암묵지**입니다.

숙련 작업자들은 몇 년~몇십 년 동안 일하면서 자신만의 작업 노하우를 갖게 됩니다.

예를 들어 물체를 자른다고 하면

```text
작업자 A
→ 여기에서 자름

작업자 B
→ 조금 여유 있게 자름

작업자 C
→ 또 다른 위치에서 자름
```

인데 모두 문제가 없을 수 있습니다.

즉,

> **정답이 하나가 아닐 수 있습니다.**

이런 지식은 문서에도 정확히 적혀 있지 않은 경우가 많습니다.

그래서 AI 개발자가 직접 작업자를 인터뷰합니다.

```text
왜 여기서 자르세요?

이 상황에서는 왜 다르게 하세요?

이렇게 하면 어떤 문제가 있나요?
```

같은 질문을 합니다.

---

## 13. AI Operator의 또 다른 의미 = 암묵지의 표준화

```text
작업자 A 방식
작업자 B 방식
작업자 C 방식
        ↓
현장 인터뷰
        ↓
공통 기준 도출
        ↓
AI Operator
        ↓
작업 표준화
```

즉 AI Operator는 단순히

> “사람 대신 AI가 설비를 움직인다.”

가 아니라,

> **사람들의 암묵적인 노하우를 데이터와 시스템으로 정리해 표준화하는 과정**

이라고도 볼 수 있습니다.

---

## 14. 현장 인터뷰가 중요한 이유

자료만 받고 개발하면 실패할 가능성이 높습니다.

개발자 생각:

> 이렇게 움직이면 되겠지.

현장 작업자:

> 실제로 우리는 그렇게 안 하는데?

가 될 수 있기 때문입니다.

그래서 발표자가 굉장히 강조한 것이

> **현장에 직접 가라.**

입니다.

현장의 VOC를 듣고 작업자의 생각을 확인한 다음 AI가 어떻게 동작해야 할지 정해야 합니다.

---

## 15. 개발 범위 관리

실제 프로젝트에서는 흔히 이런 상황이 생깁니다.

```text
처음 요구사항
A

개발 중
A + B

조금 지나서
A + B + C

또 지나서
A + B + C + D
```

계속 기능이 추가됩니다.

그래서 PM이 중요합니다.

발표자가 선호하는 PM은

> 되는 것과 안 되는 것을 명확하게 말하는 PM

입니다.

예를 들어

> A 기능부터 완성하고 B 기능은 고도화 단계로 넘기겠습니다.

라고 범위를 관리해야 합니다.

---

## 16. 개발 기간은 보수적으로 잡아야 한다

개발자는

> “한 달이면 할 수 있을 것 같은데?”

라고 생각할 수 있습니다.

하지만 실제 현장 프로젝트에는 개발 외의 변수가 많습니다.

```text
네트워크 승인
보안 승인
카메라 설치
PLC 협의
서버 설치
현장 출입
타 부서 협조
```

등입니다.

그래서 코드 자체는 1주일이면 끝나도 프로젝트는 훨씬 오래 걸릴 수 있습니다.

---

## 17. 질문과 기록이 중요하다

신입사원이 입사하면 회의에 많이 데려간다고 합니다.

처음에는 대부분 이해하기 어렵습니다.

그래도 회의록을 작성시킵니다.

```text
회의 참석
↓
내용 정리
↓
모르는 용어 찾기
↓
질문
↓
도메인 지식 증가
```

그리고 더 중요한 이유는 **책임과 요구사항을 명확하게 남기기 위해서**입니다.

예를 들어 현업이

> 이 상황에서는 A 방식으로 하죠.

라고 말했다면 반드시 기록합니다.

나중에

> 왜 이렇게 개발했나요?

라는 문제가 생겼을 때

> 당시 A 방식으로 협의했습니다.

라고 확인할 수 있습니다.

---

## 18. 모델이 아니라 시스템을 봐야 한다

연구에서는

```text
Accuracy
Precision
Recall
mAP
```

같은 모델 성능이 굉장히 중요합니다.

하지만 현장에서는 이것만 보면 안 됩니다.

```text
Camera
 ↓
Network
 ↓
Server
 ↓
AI Model
 ↓
Application
 ↓
PLC
 ↓
설비
```

전체가 연결되어 있기 때문입니다.

하나만 문제가 생겨도 전체 시스템이 멈출 수 있습니다.

그래서 현장 개발자는

> **AI Model Engineer + System Engineer 관점**

을 같이 가져야 합니다.

---

## 19. Docker를 사용하는 이유

공장 내부는 인터넷이 차단되어 있는 경우가 많습니다.

그러면

```bash
pip install ...
```

로 필요한 라이브러리를 바로 받을 수 없습니다.

그래서 Docker를 사용합니다.

```text
Python
PyTorch
CUDA
OpenCV
AI Model
각종 Library
```

와 함께 Docker Image로 만든 다음 현장 서버에 그대로 배포합니다.

```text
개발실 환경
      ↓
Docker Image
      ↓
공장 서버
```

그래서

> “내 컴퓨터에서는 되는데 현장 서버에서는 안 돼요.”

같은 문제를 크게 줄일 수 있습니다.

발표자는 신입이라면 Docker와 Linux를 능숙하게까지는 아니더라도 최소한 사용해본 경험이 있으면 좋다고 봅니다.

---

## 20. 프로젝트 A — 철도 건널목 안전관제

대표적으로 YOLO 같은 객체 탐지 모델을 사용합니다.

```text
CCTV
 ↓
YOLO
 ↓
차단기 탐지
사람 탐지
차량 탐지
 ↓
건널목 위험 판단
```

예를 들어

```text
차단기 내려감
+
건널목 내부 차량 존재
=
위험
```

으로 판단할 수 있습니다.

---

## 21. 안전관제에서는 미탐이 특히 위험

### 오탐

실제로 위험하지 않은데 위험하다고 판단.

```text
AI: 위험!
실제: 정상
```

### 미탐

실제로 위험한데 AI가 놓침.

```text
AI: 정상
실제: 위험
```

안전 시스템에서는 **미탐이 훨씬 위험합니다.**

따라서 안전관제에서는

> 미탐을 최대한 줄이는 방향

으로 시스템을 설계합니다.

---

## 22. 안전관제에서 최적화

이 프로젝트는 제한된 서버 자원에서 실행해야 하기 때문에 모델 최적화가 중요했다고 설명합니다.

발표 전사상 표현을 보면 **TensorRT 등의 최적화**를 사용한 것으로 보입니다.

```text
YOLO Model
↓
Optimization
↓
TensorRT 등의 Runtime
↓
추론 속도 증가
↓
더 많은 CCTV 처리
```

즉 모델 정확도뿐 아니라 **FPS / GPU Memory / 동시 CCTV 처리량**도 중요한 성능 지표가 됩니다.

---

## 23. 프로젝트 B — AI Operator

```text
             Main Agent
                 │
      ┌──────────┼──────────┐
      ↓          ↓          ↓
 Sub Agent A  Sub Agent B  Sub Agent C
      │          │          │
     Tool       Tool       Tool
      │          │          │
      └──────────┼──────────┘
                 ↓
               PLC
                 ↓
               설비
```

AI Agent와 Vision, 설비 제어가 섞여 있는 구조입니다.

---

## 24. LangChain / LangGraph 기반 Multi-Agent

발표에서는 Agent Framework로

- LangChain
- LangGraph

등을 언급합니다.

Main Agent가 요청을 받고 작업 종류를 판단한 뒤 적절한 Sub Agent에게 일을 맡깁니다.

```text
사용자 / 설비 Trigger
        ↓
Main Agent
        ↓
작업 종류 판단
        ↓
Vision Agent
PLC Agent
Data Agent
        ↓
Tool 호출
        ↓
실제 작업
```

이를 **Multi-Agent / Orchestration 구조**라고 볼 수 있습니다.

---

## 25. Harness Engineering

LLM에게

> 이렇게 해.

라고 말한다고 항상 정확하게 그렇게 행동하지 않습니다.

그래서 현장에서는 많은 가드를 둡니다.

```text
이 조건이면
→ 반드시 A

이 조건이면
→ B 금지

이 작업 전에
→ 반드시 확인

이 값 이상이면
→ 중지
```

발표자는 이런 것을 **Harness Engineering**이라고 설명합니다.

겉으로 보면 Rule-based 시스템처럼 보일 정도로 많은 안전장치를 둡니다.

---

## 26. Rule-based vs Agent

현업에서는

> “Trigger 받아서 작업하는 거면 그냥 Rule-based로 하면 되는 거 아닌가요?”

라고 물을 수 있습니다.

Rule-based라면

```python
if A:
    B()
elif C:
    D()
```

처럼 만들 수 있습니다.

단순한 상황에서는 Rule-based가 더 싸고 안정적일 수도 있습니다.

하지만 Agent를 사용하는 이유는 **예상하지 못한 상황에 대한 판단 능력** 때문이라고 발표자는 설명합니다.

현재는

```text
Agent
+
많은 Guard Rule
```

형태이지만, LLM/VLM이 발전하면

```text
Agent
+
적은 Guard Rule
```

로 점차 발전할 수 있다는 관점입니다.

---

## 27. PLC와 AI 연결에서 Latency가 중요

AI Operator는 단순한 Chatbot이 아닙니다.

```text
AI 판단
↓
PLC
↓
설비 동작
```

이 구조에서 네트워크 지연이 크면 위험합니다.

예를 들어

```text
AI: 지금 잘라!
↓
Network
↓
1초 후
↓
PLC 동작
```

이라면 제조 공정에서는 1초가 매우 큰 문제가 될 수 있습니다.

그래서 가능한 한 네트워크 경로를 짧게 하고 공장 내부에서 직접 연결해 Latency를 줄이는 방향을 고민합니다.

---

## 28. PLC는 특히 조심해서 다룬다

```text
잘못된 명령
↓
설비 오동작
↓
제품 손상
↓
설비 손상
↓
안전 문제
```

로 이어질 수 있기 때문에, 새로운 기능을 PLC에 마음대로 추가하기보다 **기존 PLC 신호를 최대한 활용하는 방향**을 선호한다고 설명합니다.

---

## 29. POC와 실제 프로젝트의 차이

POC에서는

```text
좋은 카메라
좋은 화각
원하는 데이터
통제된 환경
```

을 만들 수 있습니다.

하지만 실제 적용은

```text
기존 카메라
이상한 화각
노후 서버
보안 정책
네트워크 제약
현장 환경
```

을 사용해야 할 수 있습니다.

그래서

> **POC 성공 ≠ 현장 프로젝트 성공**

입니다.

---

## 30. 현장에서 쓰는 오픈소스 LLM

Q&A에서는 성능 좋은 **오픈소스 LLM을 가져와 사내에서 가공하여 사용하는 방식**을 설명합니다.

```text
Open-source LLM
↓
연구소
↓
Fine-tuning / Post-training
↓
제조 도메인 특화
↓
On-premise
```

그리고 사내 업무에서는 **RAG도 많이 사용한다**고 언급합니다.

---

## 31. 생성형 AI와 바이브 코딩

포스코DX 내부에서도 생성형 AI 사용이 점점 확대되고 있다고 합니다.

다만 중요한 것은

> **AI가 짜준 코드를 그냥 복사해서 사용하면 안 된다.**

는 것입니다.

```text
AI가 코드 생성
↓
코드 구조 확인
↓
왜 이렇게 작성했는지 이해
↓
모르는 부분 질문
↓
수정
```

해야 한다는 것입니다.

---

## 32. 신입사원에게 기대하는 것

발표자의 부서 기준으로 최근 Vision AI 인력이 부족해 신입에게도 비교적 빠르게 업무를 맡길 수 있다고 합니다.

다만 처음부터 모든 제조 공정을 알기를 기대하는 것은 아닙니다.

회사에 들어가면 공정 관련 내부 교육을 받으면서 필요한 도메인 지식을 배웁니다.

---

## 33. 취업 준비에서 중요하게 본 것

발표자 본인의 사례:

- 코딩에 자신 있었음
- Java 코딩 테스트 성적이 좋았음
- Big Data 교육 경험
- AI 교육 경험
- 프로젝트 수상 경험

중요한 메시지는

> 자격증 숫자만 늘리는 것보다 **“내가 실제로 잘하는 것이 무엇인지 증명할 수 있어야 한다.”**

는 것입니다.

---

## 34. 발표자가 신입에게 특히 강조한 능력

| 영역 | 필요한 역량 |
|---|---|
| Coding | Python / 기본 개발 능력 |
| Vision | Object Detection 등 |
| LLM | Agent / RAG 기본 이해 |
| System | Linux |
| Deployment | Docker |
| 협업 | Git |
| Data | 데이터 분석 |
| 제조 | 현장/공정 이해 |
| Communication | 현업 인터뷰 |
| Documentation | 회의록/요구사항 기록 |
| Optimization | GPU/Inference 최적화 |
| Operation | 서버·네트워크·장애 대응 |

즉 포스코DX에서 말하는 AI 개발자는 단순히

> **모델 학습시키는 사람**

보다는

> **AI 모델을 실제 제조 시스템 안에 넣어서 돌아가게 만드는 사람**

에 더 가깝습니다.

---

## 발표 전체를 한 그림으로 정리

```text
                    [ 제조 현장 ]
                         │
                 Camera / Sensor
                         │
             ┌───────────┴───────────┐
             │                       │
        Vision AI               Sensor Fusion
       YOLO 등                 Camera + LiDAR
             │                       │
             └───────────┬───────────┘
                         ↓
                    상황 인식
                         ↓
                 LLM / VLM Agent
                         ↓
                  Main Agent
                         ↓
                Multi-Agent 구조
              ┌────────┼────────┐
              ↓        ↓        ↓
           Vision    Data      Control
           Agent     Agent      Agent
              │        │        │
              └────────┼────────┘
                       ↓
                      PLC
                       ↓
                     설비
                       ↓
                   자동 제어
```

이것을 실제로 운영하기 위해 바깥에서 다음 요소들이 받쳐줍니다.

```text
온프레미스
Edge Server
GPU 최적화
Docker
Linux
Network
보안
현장 인터뷰
요구사항 관리
문서화
```

---

## 발표 전체를 한 문장으로 요약

> **스마트팩토리 AI의 핵심은 가장 좋은 AI 모델을 만드는 것이 아니라, 불완전한 현장 데이터와 제한된 하드웨어·보안·설비·사람이라는 현실적인 제약 속에서 AI를 실제로 안정적으로 돌아가게 만드는 것이다.**

교육에서 `YOLO`, `LLM`, `RAG`, `Agent`를 배우더라도 포스코DX 현장에서는 그 기술 하나만 보는 것이 아니라

**데이터 수집 → 모델 → 최적화 → 서버 → Docker → Network → PLC → 현장 운영 → 작업자 피드백**

까지 하나의 시스템으로 본다는 점이 이 발표의 핵심입니다.
